export default function Page() {
  return (
    <main style={{ padding: 32, fontFamily: "system-ui, sans-serif" }}>
      <h1>VedaAI — scaffold up.</h1>
      <p>
        Phase 0 deploy verification. Real UI lands in Phase 3 (frontend
        shell) and Phase 4 (four screens) per doc/EXECUTION_PLAN.md.
      </p>
    </main>
  );
}
